/*import 'mocha';
import { app } from '../../../app';
import * as chai from 'chai';
import chaiHttp = require('chai-http');

chai.use(chaiHttp);

describe('Test the user API', () => {

    it('Test the function to get a user', () => {
        chai.request(app)
            .get("/api/v0/users/1")
            .end(function (err, res) {
                chai.expect(res).to.have.status(200);
            });
    });

});*/